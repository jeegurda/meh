
You'll need npm to build this project.

1. Install npm (https://www.npmjs.com/).
2. Install all packages:

	$ npm i

3. To compile the project into /dist directory:

	$ gulp build

   You can also continuously rebuild the project if any of /src files were changed, like so:

   	$ gulp watch